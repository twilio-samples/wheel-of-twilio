// Surface tokens shared with /stats so both back-of-house screens read as
// one system, distinct from the main wheel screen's felt background.
export const SURFACE = {
  card: "#0B1B3E",
  raised: "#12275A",
  line: "#24365F",
};
